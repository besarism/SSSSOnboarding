import XCTest
@testable import SSSSOnboarding

final class SSSSOnboardingTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSSSOnboarding().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
